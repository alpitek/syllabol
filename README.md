# syllabol

a service to generate syllables

## sbt project compiled with Scala 3

## Requirements

- JDK 21 LTS
- sbt 1.10.6
- Scala 3.3.6

### Usage

This is a normal sbt project.
Compile code with `sbt compile`, run it with `sbt run`, and `sbt console` will start a Scala 3 REPL.
